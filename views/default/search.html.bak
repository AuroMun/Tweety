{{extend 'layout.html'}}
<h2>Search for birdies to follow:</h2>
{{=form}}

{{if people:}}
<h3>
    Birdies:
</h3>
{{for user in people:}}
{{=A(user.auth_user.first_name, _href=URL('profile', args=user.auth_user.id))}}
{{if user.auth_user.id in user.followers.followee:}}
	<button onclick="ajax('{{=URL('follow',args=('unfollow',user.auth_user.id))}}',[],null);$(this).parent().html('Unfollowed')">Unfollow</button>
    {{else:}}
    <button onclick="ajax('{{=URL('follow',args=('follow',user.auth_user.id))}}',[],null);$(this).parent().html('Followed')">Follow</button> 
{{pass}}
{{pass}}
{{pass}}
